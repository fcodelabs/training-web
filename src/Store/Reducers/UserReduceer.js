/*
const initialState ={uname: "Default Name"}

const  nameReducer = (state = initialState, action)=>{
    console.log("action check",action)
    switch(action.type){
        case 'regUser':

        return state.uname = "asd"


        default:
          
            return state
    }
  
}

export default nameReducer;

*/